{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf node_modules/.prisma && prisma generate && prisma migrate deploy && next build",
  "installCommand": "npm install --ignore-scripts",
  "framework": "nextjs",
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  }
}
